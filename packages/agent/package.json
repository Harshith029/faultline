{
  "name": "@faultline/agent",
  "version": "1.0.0",
  "description": "Continuous cascade-failure detection agent: ingests telemetry, runs rolling detection, manages incidents, alerts",
  "type": "module",
  "license": "MIT",
  "bin": {
    "faultline": "./bin/faultline.js"
  },
  "main": "./src/agent.js",
  "scripts": {
    "start": "node bin/faultline.js start",
    "demo": "node bin/faultline.js start --config faultline.demo.json",
    "test": "node --test"
  },
  "dependencies": {
    "@faultline/core": "*"
  },
  "optionalDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
